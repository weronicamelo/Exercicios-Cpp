#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

int main(){
    
    unsigned seed = time(0);
    srand(seed);

    for(int i = 1; i < 10; i++){
    cout << (rand()%10) + 1 << endl;
    }
    
    return 0;
}